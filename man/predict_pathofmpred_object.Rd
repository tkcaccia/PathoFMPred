% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_pathofmpred_object}
\alias{predict_pathofmpred_object}
\title{Apply a user-created PathoFMPred object}
\usage{
predict_pathofmpred_object(
  object,
  features,
  patient_id = NULL,
  id_column = NULL,
  outcome_type = c("continuous", "binary"),
  endpoints = NULL,
  aggregation = NULL,
  warn_ood = TRUE
)
}
\arguments{
\item{object}{A `PathoFMPredObject` or the path to an `.rds` file containing
one.}

\item{features}{A data frame or matrix containing the named features used to
fit `object`. An identifier column may be selected with `id_column`.}

\item{patient_id}{Optional identifier per feature row. Use either
`patient_id` or `id_column`, not both. Repeated identifiers are pooled.}

\item{id_column}{Optional identifier-column name in `features`.}

\item{outcome_type}{One or both of `continuous` and `binary`.}

\item{endpoints}{Optional endpoint names to apply.}

\item{aggregation}{Optional `mean` or `median` override. By default the
object's model-development aggregation is used.}

\item{warn_ood}{Warn when more than 5 percent of dimensions are outside the
fitted training range.}
}
\value{
A long data frame with one row per patient and fitted endpoint.
  Binary outputs include the original event-class label. Scores are
  uncalibrated and are not probabilities.
}
\description{
This function applies an object returned by [create_pathofmpred_object()].
It is the supported inference route for locally constructed TITAN objects
and for future foundation-model representations that are not distributed by
the package. Repeated feature rows are pooled using the aggregation rule
recorded when the object was created, unless `aggregation` is supplied.
}
