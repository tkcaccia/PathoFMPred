% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_object.R
\name{create_pathofmpred_object}
\alias{create_pathofmpred_object}
\title{Build a portable PathoFMPred model collection}
\usage{
create_pathofmpred_object(
  feature_table,
  outcome_table,
  id_column,
  aggregation = c("mean", "median"),
  foundation_model = "custom",
  feature_columns = NULL,
  outcome_columns = NULL,
  outcome_types = NULL,
  positive_class = NULL,
  control = pathofmpred_control(),
  output_file = NULL
)
}
\arguments{
\item{feature_table}{Data frame, matrix, or CSV path containing identifiers
and numeric foundation-model features.}

\item{outcome_table}{Data frame or CSV path containing one unique row per
identifier and one or more outcomes.}

\item{id_column}{Name of the identifier column present in both tables.}

\item{aggregation}{How repeated feature rows are pooled: `mean` or `median`.}

\item{foundation_model}{Label stored in the resulting object.}

\item{feature_columns}{Optional numeric feature columns. By default all
columns except `id_column` are used.}

\item{outcome_columns}{Optional outcomes to fit. By default all columns
except `id_column` are considered.}

\item{outcome_types}{Optional named character vector containing `continuous`
or `binary`. Otherwise numeric two-level outcomes are binary and other
numeric outcomes are continuous.}

\item{positive_class}{Event-class label for binary outcomes. Supply either
one value when fitting a single binary outcome or a named character vector
keyed by endpoint. Zero/one and FALSE/TRUE outcomes default to one and TRUE;
all other binary labels require an explicit value.}

\item{control}{Settings created by [pathofmpred_control()].}

\item{output_file}{Optional `.rds` destination.}
}
\value{
A `PathoFMPredObject` containing fitted models, a registry, nested-CV
  summaries, and a linkage/missingness audit.
}
\description{
Feature and outcome tables are linked only through `id_column`. Outcome IDs
must be unique. Feature IDs may repeat, for example when a patient has more
than one slide, and are pooled before model development.
}
