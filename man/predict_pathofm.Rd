% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_pathofm}
\alias{predict_pathofm}
\title{Apply supported models for one pathology foundation-model representation}
\usage{
predict_pathofm(
  cancer,
  features,
  foundation_model = c("TITAN", "GigaSSL", "ProvGigaPath"),
  patient_id = NULL,
  outcome_type = c("continuous", "binary"),
  family = NULL,
  warn_ood = TRUE,
  include_limited_evidence = FALSE
)
}
\arguments{
\item{cancer}{One TCGA cancer code per row, or one code recycled to all rows.}

\item{features}{A matrix or data frame containing the correctly named
features for `foundation_model`.}

\item{foundation_model}{One of `TITAN`, `GigaSSL`, or `ProvGigaPath`.}

\item{patient_id}{Optional identifier per row. Repeated identifiers are pooled.}

\item{outcome_type}{One or more of `continuous` and `binary`.}

\item{family}{Optional endpoint-family filter.}

\item{warn_ood}{Warn when over 5 percent of dimensions fall outside training ranges.}

\item{include_limited_evidence}{Explicitly opt in to limited-sample models
that still crossed the representation-specific matched effect threshold.
Models tested below the threshold never enter prediction or reporting.}
}
\value{
A long data frame with one row per patient and applicable model.
}
\description{
Multiple rows with the same `patient_id` are interpreted as multiple slides
and mean-pooled before prediction, matching model development.
}
